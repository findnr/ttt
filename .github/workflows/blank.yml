name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: test time
      run: |
        curl -s https://install.zerotier.com | sudo bash
        sudo zerotier-cli join ${{ secrets.CYM_TOKEN }
        while true ;do \
            echo 'helloword' \
            sleep(10) \
        done 
