name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: test time
      env:
        SUPER_SECRET: ${{ secrets.ZEROTIER_ID }}
      run: |
         ls -a
         who
