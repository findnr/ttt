name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: test time
      run: |
        curl -s https://install.zerotier.com | sudo bash
        zerotier-one -v
