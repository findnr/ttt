name: CI
on:
  workflow_dispatch:
jobs:
  task-1:
    uses: ${{ github.repository }}/.github/workflows/task.yml@main
  task-2:
    needs: task-1
    uses: ${{ github.repository }}/.github/workflows/task.yml@main
  task-3:
    needs: task-2
    uses: ${{ github.repository }}/.github/workflows/task.yml@main
