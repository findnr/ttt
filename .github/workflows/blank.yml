name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: test time
      run: |
        date -R
    - name: Setup tmate session
      uses: mxschmitt/action-tmate@v3
